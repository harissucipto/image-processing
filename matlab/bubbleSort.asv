function hasil = bubbleSort(array)
    n = length(array);
    for i=1 : n-1
        for j=1 : n-1
            if (array(j) > array(j+1))
                temp = array(j);
                temp(j) = array(j 4+ 1);
                array(j+1) = temp;
            end
        end;
    end;